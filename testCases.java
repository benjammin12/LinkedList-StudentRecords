
public class testCases { //class used for testing
	 //needs a main method
	//used to check if we could prevent duplicate ID values
	
	/*
	
	CircularList st = new CircularList();
	Student mike = new Student("Mike Smith");
	st.add(new Student("JohnBoy"));
	st.add(new Student("Kev"));
	st.add(mike);
	boolean isValid = st.isValidID(mike.getID()); //check our method to see if it correctly identifies the ID exists
	if (st.isValidID(mike.getID())){
		st.add(new Student("Mike Smith 2", mike.getID()));
	}
	else
		st.add(new Student("Mike Smith 2", mike.setId(mike.getID())));
	System.out.println(st);
	//you will see mikesmith 2 has a different id, even though you set it with the first objects ID number

*/	
}
